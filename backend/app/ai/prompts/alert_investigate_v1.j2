List 3-6 concrete, prioritized investigation steps a SOC analyst should take
next for this alert. Be specific — name commands, log sources, or systems to
check — rather than generic advice like "investigate further."

{{ untrusted_alert }}

Respond with ONLY JSON matching this schema:
{"steps": ["string", "string", ...]}
